spring:
  application:
    name: lifeos-backend
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

notion:
  # BYOK: the token and root-parent-page-id are supplied per call (CLI options /
  # REST request body), never read from environment or config here.
  version: ${NOTION_VERSION:2026-03-11}
